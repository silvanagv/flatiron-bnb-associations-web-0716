<h1>City Form</h1>

<%=form_for(@city) do |f| %>
  <label>Name</label>
  <%= f.text_field :name %>
    <label>Neighborhoods</label>
  <%= f.collection_check_boxes :neighborhood_ids, @neighborhoods, :id, :name %>
  <%=f.submit %>
<% end %>
